services:
  app_x:
    image: nginx:alpine
    container_name: app_x
    restart: unless-stopped
    networks:
      - traefik-net
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    labels:
      - "traefik.enable=true"
      - "traefik.http.services.app-x.loadbalancer.server.port=80"

networks:
  traefik-net:
    external: true
